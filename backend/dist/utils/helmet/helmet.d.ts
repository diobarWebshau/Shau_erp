import type { RequestHandler } from "express";
/**
 * Helmet devuelve funciones que son middlewares de Express
 * ⇒ El tipo correcto es RequestHandler[]
 */
declare const helmetUtil: RequestHandler[];
export default helmetUtil;
